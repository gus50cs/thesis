#!/bin/bash

# Set the value of the ports variable
ports="1 2 3 4 5"

# Loop through each port in the ports variable
for port in $ports
do
    # Print a message for each port
    echo "Processing port: $port"
    echo "hello"
done
